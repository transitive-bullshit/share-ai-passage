# Phase 2 implementation review

September 15, 2026. **Initial implementation: `e5f43dd`; recorded source Preview checkpoint: `e0182daf81fe22daf11684ecfd9bef1ee802db2e`**, on `codex/accounts-first-launch`. Accounts were committed and Gate A approval recorded in `15755bc` before paid work began. The shared Preview is deployed; **Gate B has not passed**. This rollout has not connected to, migrated or deployed production, and no live payment occurred.

## Implemented

- Better Auth/Stripe subscriptions and Portal integration, authoritative payment entitlements, monthly allowances for annual plans, and the $10/50 image pack. Original prices and quotas remain unchanged.
- Saved templates, private R2 uploads/references/artwork, custom branding and typography, frozen public cards, and exact-design owned revisions.
- Durable image operations through Workflow, one provider-submission claim, credit/spend accounting, uncertain-result recovery, deletion races and explicit result application after edits.
- Revocable account API keys, authenticated CLI/skill defaults and durable recovery files, preserving anonymous use.
- Explicit operator commands for summary/image/billing reconciliation and private-asset cleanup, plus a read-only aggregate operations check with optional Resend digests. The daily private-cleanup schedule is declared; the operations-email schedule and recipient remain unconfigured.

## Verification

The final implementation sources passed `pnpm test`: **752 tests in 63 files**, with all PostgreSQL suites enabled against the migrated disposable local database. Formatting, oxlint and Next route generation/TypeScript passed. A separate production build passed with six Workflow steps and one workflow. External service credentials were cleared for these checks; provider responses in tests were fixtures.

Production HTTP smoke used the build at `127.0.0.1:3109`, the isolated test database and an explicit fixture auth secret. Homepage/billing/templates/keys returned 200; unauthenticated billing/templates/key APIs returned 401 with private/no-store responses. The native example-card route returned a valid 1200×630 WebP. The temporary server was stopped afterward. Saved evidence: `work/phase2/production-http-smoke.json`.

An actual local Workflow run completed through the generated flow/step routes using a pre-cancelled synthetic operation. It made no model request. Its temporary database records were removed. Saved evidence: `work/phase2/workflow-smoke.json`. This establishes local queue/routing behavior, not hosted execution or paid provider recovery.

Browser review on the isolated HTTPS accounts app covered monthly/annual prices, unavailable-service messaging, a temporary Plus draft, watermark removal, typography changes, template saving/listing, and persistence after reload. The live card's fonts and artwork were loaded, Modern typography survived reload, and the 390px mobile breakpoint had no horizontal overflow. Generated mode blocked publishing while artwork was missing; disabled generation returned a clear error and created zero image/summary operations. The temporary Plus entitlement, draft and template were removed, restoring the original Free account.

Eight legacy WebP exports remained byte-identical to their frozen baseline. Five paid export fixtures were visually checked; the v2 image sample was also rendered in all five layouts. The resumed paid-draft regression changes repository geometry and confirms the saved frozen descriptor survives unchanged. The local saved HTML comparison at port 4399 was blocked by the browser client and was not claimed as reviewed; live preview and exported-image checks are recorded separately.

## Draft recovery follow-up

The follow-up on September 15 makes create, browser reopening and CLI resume continue an untouched draft's initial template image when summary preparation finished before image scheduling. Automatic recovery follows the latest accepted operation for that draft/revision, including an explicit replacement, failed or uncertain work. Request-key collision checks and a second check under the account usage lock prevent recovery from reserving another generation. Explicit rerolls retain their own request identity; read-only status polling never starts a new generation.

The updated sources passed the complete `pnpm test` checks: **767 tests in 64 files**, with the isolated PostgreSQL suites enabled, plus formatting, lint and TypeScript. The production build passed again with six Workflow steps, one workflow and 35 static pages. Regression coverage includes delayed summary readiness, interrupted browser/CLI recovery, concurrent reservations, disabled generation configuration and a failed initial job followed by an uncertain explicit replacement. No additional paid provider requests or production changes occurred.

## Real-service development checks

September 15 follow-up: the **Passage Development** Stripe sandbox under Agentic (`acct_1UFoiGFCzsOIkvWC`) now has the five expected USD prices, a default management Portal and a separate guarded upgrade-confirmation configuration. The `passage-development` CLI profile's test credential expires December 14, 2026. Catalog evidence is `work/phase2/stripe/catalog-evidence.json`. The main ignored development environment, isolated review configuration and shared Vercel Preview reuse these settings; production remains unchanged.

Completed **sandbox browser** billing checks:

- $10 Plus Checkout completed with three signed webhook deliveries returning 200; the app showed 100 monthly summaries and 10 included images.
- A $10 pack purchase added exactly 50 purchased image credits.
- The Plus-to-Pro confirmation displayed a $14.99 prorated test invoice, completed payment, and the app showed Pro with 300 summaries/25 included images plus the existing 50 purchased credits. This observed charge depends on remaining time in the billing period.
- General Manage billing showed invoice history, payment details and cancellation, with no plan-change action. Only the server-selected confirmation configuration permits guarded same-cadence upgrades. Scheduled changes and pending subscription payments return actionable conflicts without being replaced.
- Cancellation and **Keep current plan** restoration passed after handling Stripe's timestamp-only `cancel_at` state. The app showed the cancellation correctly and retained Pro access with 25 included images through cancellation/restoration.
- A Plus annual downgrade/cadence change was saved for October 15 and appeared after **Refresh billing**. The initial return was stale; the schedule was not lost. **Keep current plan** then released the schedule. The final refreshed state is monthly Pro with 300 summaries, 25 included images and zero purchased credits, with no pending/cancellation notice or Keep current plan action.
- Sandbox branding was saved: the existing Passage icon, white background, charcoal buttons, Inter and default rounded Checkout styling. SDK readback confirmed the icon and account colors; the Portal preview visually matched. **Passage Development** remains the sandbox display name; live business onboarding was left untouched. Evidence: `work/phase2/stripe/branding-evidence.json` and `BRANDING.md`.

No live payment occurred. Local events are forwarded to backend 3107. Shared Preview now also has its separately signed sandbox endpoint; hosted delivery remains blocked as recorded below. Listener restart and two-configuration setup instructions are in the [production guide](../PRODUCTION.md#stripe). Better Auth Stripe 1.7.4 emitted a missing/null subscription log for the one-time pack event; the handler returned 200 and the credit was correct. This remains a known nonblocking upstream log. The saved `confirm-session-evidence.json` covers an earlier session-creation probe; it is not the later browser payment evidence.

Actual Stripe API lifecycle checks also passed against the isolated review database. A synthetic Plus subscription on a Stripe test clock automatically paid its second $10 monthly invoice; direct reconciliation recognized the renewed coverage and 10 included images using the app's real wall clock. Replaying the same event and refreshing authoritative state did not duplicate the grant. The authorized full refund of the review account's $10 pack revoked its 50 credits, leaving purchased balance **zero** while preserving Pro access and used/reserved counters. Reconciliation replay was idempotent. The synthetic clock/customer/subscription and exact database fixtures were cleaned up; the existing review account, Pro subscription and refunded grant were retained. Evidence: `work/phase2/stripe/lifecycle-smoke/evidence.json` and `README.md`.

Separately, the local listener recorded signed `invoice.paid`, `charge.refunded` and `refund.updated` deliveries returning 200. The direct reconciliation harness and listener ran concurrently, so these checks establish both paths' recorded behavior without attributing every database change to one path. They do not establish hosted webhook delivery; later lifecycle checks below have their own evidence.

The reconciler and listener now handle all six subscription-schedule lifecycle events, bringing the listener to 20 billing event types. A metadata-only `subscription_schedule.updated` event (`evt_1UFpikFCzsOIkvWCjjhmE06q`) returned 200 through the signed endpoint. Restoring the plan also delivered `subscription_schedule.released` (`evt_1UFpjPFCzsOIkvWCAQc8rNKG`), `customer.subscription.updated` and `subscription_schedule.updated`, all with 200 responses. This covers schedule changes independently of subscription events; the transition check below provides separate evidence.

A separate synthetic failed-renewal check automatically declined the next $10 Plus invoice after previous paid coverage had expired. Direct reconciliation of the actual `invoice.payment_failed` event reported Free, 25 summaries and zero image credits, and rejected new paid actions. Replacing the test payment method and paying the same invoice restored confirmed Plus coverage, 100 summaries and 10 images. Duplicate paid-event replay, replay of the already processed failure, and authoritative refresh preserved one image grant and its counters. The app used real wall time throughout; the earlier paid period was already expired, so this was not an observed real-time expiry transition. Independent readback confirmed deletion of the synthetic clock/customer, cancellation of its subscription and zero exact fixture rows. Evidence: `work/phase2/stripe/renewal-recovery-smoke/evidence.json` and `README.md`. This does not establish browser recovery, automatic retry scheduling, previously unseen out-of-order events or hosted delivery.

**Scheduled annual transition defect:** reproducing Better Auth Stripe 1.7.4's deferred Pro-monthly → Plus-annual payload changed the price but created no $96 annual invoice: the annual period incorrectly retained the original Pro billing anchor. Passage correctly withheld unpaid annual access. A separate real-Stripe comparison added only `billing_cycle_anchor: 'phase_start'` to the second phase, preserving `proration_behavior: 'none'`. Stripe then paid the $96 invoice for the correct annual period. After waiting for the actual boundary, reconciliation using the unchanged app clock switched to Plus/year and cleared the pending change. The original allowance anchor and seeded prior usage were preserved; replay did not duplicate grants. Two later monthly allowance windows were checked by passing explicit future dates to the existing usage-service parameters, not by changing the app clock or waiting two real months. Those reads yielded fresh 10-image/100-summary allowances without rollover or duplicate grants, while a real-time read excluded future grants. Both synthetic fixtures were cleaned up. Evidence: `work/phase2/stripe/annual-transition-smoke/README.md`, original `evidence.json`, and `phase-start/evidence.json`. The maintained one-line pnpm patch is installed and locked. The authenticated native-route regression failed before the patch on the missing anchor and passed afterward, including native schedule persistence and unchanged current paid access. Full `pnpm test` passed **804 tests in 66 files** with the isolated PostgreSQL suites, formatting, lint and TypeScript; the production build passed with 36 static pages, six Workflow steps and one workflow. Docker copies the patch before its frozen install; a container build was not run because the local Docker daemon is unavailable. The sandbox API comparison and fixture-backed route test establish separate parts of the fix; neither establishes hosted behavior.

A separate synthetic pack Checkout completed in Chrome with Stripe’s public test card and USD $10 selected, granting exactly 50 purchased images. An actual $3.01 partial refund revoked `ceil(50 × 301 / 1000) = 16` credits. With 40 used units explicitly seeded in that fixture, reconciliation preserved usage and recovered the six-credit shortfall once from its 10 included credits, leaving four included, zero purchased and no outstanding debt. Replaying the real refund event and rereading usage changed no counters. Independent SDK/SQL readback confirmed cancellation/deletion of the exact synthetic customer/subscription and zero fixture rows; the existing review Pro account stayed active. No image request ran. Evidence: `work/phase2/stripe/partial-refund-smoke/state.json` and `README.md`. The local listener separately recorded signed renewal-failure, recovery-payment, pack-completion and partial-refund deliveries returning 200. Direct reconciliation and the listener ran concurrently, so database changes are not attributed solely to either path. Seeded consumption tests accounting, not actual generation or hosted delivery.

A previously unseen out-of-order check held a synthetic customer unmapped while actual older subscription-created/paid-invoice events and a newer cancellation passed the signed local listener. After mapping, the newer cancellation was reconciled first. Each old event was proven absent from the event ledger immediately before its first direct reconciliation; both were processed once without changing authoritative fields or restoring paid access. No processed row was deleted to manufacture late delivery. Fresh SDK/SQL checks verified the original event payloads/timestamps and exact fixture cleanup. Evidence: `work/phase2/stripe/out-of-order-smoke/{state.json,README.md}`. Original signed ingress and deliberately late direct reconciliation were separate checks, not hosted or signed late redelivery.

Interrupted deletion recovery also passed with a new synthetic paid account. A single local pre-network timeout caused `deleteAccountData` to return 503 while retaining both closing markers, blocking new generation, removing the private draft and refunding its undispatched summary reservation at zero model cost. The actual `cancel-closing` reconciliation CLI canceled Stripe and recorded completion; retrying deletion retained the original closing timestamp. After modeled removal of only the synthetic auth row, the retained billing tombstone canceled a real late subscription, and duplicate reconciliation remained safe. Independent readback found both subscriptions canceled, the clock/customer deleted and zero exact fixture rows. Evidence: `work/phase2/stripe/deletion-recovery-smoke/timeout/{evidence.json,post-cleanup-inspection.json}` and `README.md`. This covers the real service/CLI recovery path with a local injected timeout, not the browser deletion endpoint, hosted delivery, R2 cleanup, or an unknown provider outcome after request acceptance.

The corrected annual transition also passed failure and recovery. Stripe declined the actual $96 annual invoice; after the real wall-clock boundary, Passage showed Free/25 summaries/zero images with no unpaid annual grant. Paying that same invoice restored Plus annual coverage, 100 summaries and 10 images while preserving the original monthly allowance anchor. Replaying paid and earlier-failed events retained one grant and seeded usage of four images/six remaining. Fresh independent readback confirmed cancellation of the subscription/schedule, deletion of the clock/customer and zero exact database rows. Evidence: `work/phase2/stripe/annual-payment-recovery-smoke/{evidence.json,README.md}`. This is real sandbox payment and direct reconciliation/refresh evidence; hosted signed delivery remains unverified. The isolated review backend was then restarted with the locked dependency patch; live checkout and new image generation remain disabled.

The owner explicitly approved two $10 sandbox Checkout dispute tests using Stripe’s public test card. Both real outcomes passed after fixing a newly reproduced reconciliation defect: Stripe retains `charge.disputed=true` after a win, so the old implementation kept credits revoked. The reconciler now reads all authoritative disputes for the charge; only won or closed-inquiry outcomes permit restoration. Active/lost disputes and missing details still block restoration; lookup errors remain retryable. The same check protects subscription invoice coverage, and partial/full refund handling is preserved.

Both fixtures started with 50 pack credits and explicitly seeded 40 used units. An active dispute revoked 50, consumed the fixture’s 10 included credits toward recovery and left 30 debt. Winning restored net 20 available credits and zero debt; losing retained zero available and 30 debt. Used/reserved/recovered counters survived repeated closed and earlier-created events. The original failed won result is preserved; verification after the fix used a new real metadata-only Stripe event without deleting the old event ledger. Fresh independent SDK/SQL readback verified each synthetic customer deleted, subscription canceled and all seven exact table counts zero. Stripe retains the payment/dispute audit history. Evidence: `work/phase2/stripe/dispute-smoke/{README.md,won/state.json,lost/state.json}`. This is actual sandbox Checkout plus direct reconciliation evidence, not hosted signed delivery or real image consumption.

The dispute regression tests failed before the fix and passed afterward. The completed sources passed **806 tests in 66 files**, including all disposable PostgreSQL suites, formatting, lint and TypeScript, using `node work/phase2/run-local.mjs test test`. The separate `PASSAGE_PRODUCTION_LOCAL=1 node work/phase2/run-local.mjs test build` passed with 36 static pages, six Workflow steps and one workflow. Independent code review found no actionable issues. Production and the existing review Pro account remained unchanged.

Real R2 setup now includes the existing public `passage` bucket and newly created `passage-private`, accessed through the supplied US-jurisdiction endpoint and the supported S3 variable names. Private public access stayed disabled. CORS readback originally covered the exact main development, isolated HTTPS review and canonical hosted origins. The stable Preview origin was subsequently added while preserving all three and the existing rules; see `work/phase2/hosted-readiness/r2-cors-evidence.json`. The 15-check synthetic smoke passed public and signed-private write/read/hash, preflight, origin/size enforcement and immutable overwrite rejection; both fixture objects were deleted.

A separate application-library smoke against real private R2 and the disposable Postgres database passed upload reservation/replay, finalization/normalization/replay, owner isolation, durable stored-result recovery into the original expired image grant with one debit, and account deletion/queued cleanup. Repeating recovery preserved object hash/ETag and draft revision. All three private objects and exact synthetic database records were removed; only fixture expiry was advanced to exercise cleanup. No actual provider request, live billing or hosted Workflow ran. Evidence: `work/phase2/r2/{inspection,provisioning,smoke,app-smoke}.json` and its `README.md`.

The real-R2/disposable-Postgres publication smoke composed one immutable 1200×630 WebP (26,486 bytes) and verified matching image-handler/`r2.dev` hashes. Publication replay retained the same publication, asset and ETag with one card row and zero additional object writes. Owner deletion and source unavailability each disabled application reader/metadata/card access with zero R2 image reads, while the direct public URL retained the original bytes. The harness removed only its two private objects, public fixture card and exact database records. This direct-handler synthetic check did not exercise hosted serving or a social crawler; see `work/phase2/r2/publication-smoke.json`.

Actual browser upload, template save and reload passed. The **R2 browser smoke** template remains only in the isolated review account and was not set as its default. Fixing the template editor's missing `.live-card` wrapper restored the preview's aspect-ratio container; the browser showed loaded fonts, 1200px-wide artwork and an approximately 522×273px card. The report `work/share-card-review/template-preview-container-20260915.html` and its matching `-before/` and `-after/` captures contain eight authored cards without model calls; all eight WebP hashes were identical. All eight final exports were visually inspected, with readable text, intact artwork and clear footers; the German card retained its baseline two-line ellipsis. An independent dense-card review also passed. Chrome blocked the saved HTML gallery with `ERR_BLOCKED_BY_CLIENT`, so that gallery is not claimed as reviewed; the actual app preview was checked separately.

The final combined sources passed the full `pnpm test` run with **796 tests in 65 files**, including the isolated PostgreSQL suites, formatting, lint and TypeScript. The final production build passed with 35 static pages, six Workflow steps and one workflow. These results include the cancellation-state fix, guarded upgrade confirmation, schedule-event reconciliation, storage aliases and preview container. The earlier 752/767 totals remain historical checkpoints.

The plan comparison now uses shared desktop grid rows for headings, prices, features, actions and billing notes, keeping the current-plan button aligned with its neighbors. Monthly and annual desktop layouts and the stacked 390px mobile layout were checked in Chrome; focused formatting and lint checks passed. This layout-only follow-up did not rerun the historical full-suite/build checks above.

The public `/pricing` follow-up adds a server-rendered plan comparison, monthly/annual totals, generation and upload terms, header/footer links, and annual-cadence continuation through signup/sign-in to billing. The page uses the existing plan catalog and checkout gate. On `15806ed` plus these workspace changes, `node work/phase2/run-local.mjs test test` passed **803 tests in 66 files**, including isolated PostgreSQL, formatting, lint, and TypeScript. After the final tablet-layout and copy refinements, 43 focused pricing/billing/auth/SEO tests and the production build passed again (36 static pages, including `/pricing`). Chrome checks covered desktop monthly/annual alignment, 390px mobile and 768px tablet layouts, and annual navigation into billing without a purchase. A cookie-free local HTTP request returned 200 with all plan content, a canonical `/pricing` URL, local `noindex`, and no new session cookie. No production deployment or live charge occurred.

The expanded global footer groups Product, Your account, and Developers links, including the new pricing, template, billing, and API-key pages. Desktop and 390px layouts, the FAQ anchor, and pricing navigation were checked in Chrome. The combined pricing/footer checkpoint then passed the full **803-test / 66-file** repository checks and a fresh production build with 36 static pages. The footer remains server-rendered and adds no account or billing requests.

The owner accepts `r2.dev` for development/shared Preview and deferred the production custom domain; it is not a Preview prerequisite. Production delivery verification, hosted operational checks and actual social unfurls remain open.

## Approved image qualification batch

The owner specifically approved the frozen 25-request batch after the earlier automatic-review rejection. All 25 requests completed successfully with no retries or unknown outcomes, costing **$0.316988** at the verified list rates. The benchmark now totals **39 calls and $0.489276**, with zero outstanding liability under the original $20 cap. This is provider-usage accounting, not invoice reconciliation. No additional requests were made beyond the approved batch.

The pinned `gpt-image-2.5-flare-2026-09-08` / `passage-background-v2` sample includes 18 first results, six deliberate repeats and two input-boundary probes. Median/max latency was **12.243/23.235 seconds**. All 26 backgrounds produced five production-layout exports each: 130 valid 1200×630 WebPs. Local render median/max was 40.65/85.05ms; those timings exclude hosted execution and delivery.

Independent, unblinded review accepted **17/18 first results (94.4%)**, meeting the agreed 90% sample threshold. Cross-topic style scores were Margin notes 4/5, Electric risograph 5/5 and Midnight observatory 4/5; all six repeats were usable. The reviewer inspected the three first-result grids and 32 full-resolution exports across all five layouts. All 30 ordinary sampled overlays were readable. Root separately inspected the three grids and selected exports.

The accessibility/Margin notes first result and square-reference boundary probe imported unrelated astronaut imagery from the reference. The ordinary rejection is recorded within the passed empirical quality threshold; reliable subject isolation is not guaranteed. Both boundary probes also exposed unreadably small text at 1000/1000/637-character highlights. The local follow-up below addresses that renderer/input-policy issue without more model calls.

The first-result mean image cost was 1.25245¢; the highest boundary-probe cost was 1.87370¢. Under the existing fee, summary, billed-failure and operational-reserve assumptions, full-use paid-plan contribution is approximately 84–86%, and the $10/50 pack is 85.5% at the measured mean or 82.3% at the observed maximum. These are scenarios, not guaranteed worst-case or per-user profit. Public documentation does not provide a hard per-call cost cap for this exact image configuration; more random samples cannot prove one. Selected-model qualification and the owner-approved sampled-economics/spending-policy decision are complete. The [measured economics report](../research/PHASE2_MEASURED_ECONOMICS.md) records the per-account spending controls; actual hosted billing and operational cost reconciliation remain open.

Ignored reproducible evidence: `work/phase2/image-benchmark/{qualification-v2-manifest.json,qualification-final-verification.json,QUALIFICATION_REPORT.md,qualification-metrics.json,qualification-economics.json,qualification.html}`. The provider configuration, prices, allowances and public generation switch remain unchanged.

## Local readability and spending follow-up

The owner explicitly approved per-account AI spending protection. Paid summaries and included images now share a monthly spending ceiling; purchased images use lifetime funding from net pack payments. The existing ledgers count billed failures and unknown-cost liability, and account locks serialize admission with metering and settlement. Denial preserves advertised allowances, saved work and credits. Refunds/disputes reduce pack funding without erasing historical cost; new paid packs add headroom. No schema, credential or environment-variable change is needed. The existing billing-status CLI reports both buckets. [Ceilings and limits](../research/PHASE2_MEASURED_ECONOMICS.md#supported-cost-bounds), [operator recovery](../GENERATION_RECONCILIATION.md#per-account-ai-spending-protection).

New draft previews and new publication require at least 22px highlight text at 1200×630. Unreadable cards receive shorten-text/change-style feedback; saved-text caps and full reader content are unchanged. Existing publication reuse and legacy/frozen image delivery retain compatibility. The actual extreme probe returns 400 in all five layouts. All eight authored WebP **and HTML** outputs remain byte-identical; root visually inspected all eight final native exports, including dense and German text. No provider calls ran. Evidence: `work/share-card-review/readable-card-floor-20260915-README.md` and matching gallery/before/after artifacts. The font-ready live app was subsequently checked in Chrome: the ordinary card and 1200×630 artwork loaded, distinct 1000/1000/637-character highlights remained intact while Publish was disabled, actionable shorten-text guidance appeared in Margin notes and Electric risograph, and no misleading preview retry was offered. Restoring the original wording and Margin notes style restored a loaded preview and enabled Publish; Saved was observed. No generation or publication ran. The saved HTML gallery still returns Chrome `ERR_BLOCKED_BY_CLIENT` and is not claimed as reviewed. The same ignored evidence file records these live checks.

This follow-up passed `pnpm fix:format`, `pnpm fix:lint`, and full `pnpm test`: **837 tests in 67 files**, including disposable local PostgreSQL suites, formatting, lint and TypeScript. The production build passed with 36 static pages, six Workflow steps and one workflow. Regression coverage includes concurrent summary/image admission, denied requests without debits, original-period/guest-import ownership, cadence changes, paid/unknown failures, pack refund/dispute funding, and late metering after result recovery. The single older lifecycle expectation for an unreadable maximum-length highlight was updated while retaining title-only hard-limit/deduplication coverage. Logs: `work/phase2/spending-readability-{test,build}.log`. The source revision `acbbead79d61aa180f056c3ad797e418b8f21170` also passed [GitHub CI](https://github.com/transitive-bullshit/share-ai-passage/actions/runs/34950998147), including its build, and reached READY in Preview as `dpl_4Zg5fJdHnawQyB86jo4yEpvr5W2z`. The stable alias pointed to this deployment at that checkpoint. Fresh anonymous pricing GET and webhook POST both returned Vercel 302 redirects, so public access and webhook behavior remain unverified; subsequent authenticated Google browser checks are recorded below. No protection setting was changed. Evidence: `work/phase2/hosted-readiness/spending-readability-deployment-evidence.json`. Earlier hosted runtime checks below apply to their recorded revision.

## Operations follow-up

The existing private-asset cleanup command now excludes active jobs, live upload permissions and processing leases before applying its batch limit. Failed attempts rotate behind unattempted records, and overlapping calls do no storage work. Accepted inputs and public objects remain protected. The regressions cover starvation, failed retries, overlapping runs, account deletion during cleanup and late generated results. All database tests that invoke global cleanup share one test file to prevent interference between their storage fixtures.

Image stale-job reporting now uses the operation's original age, so repeated recovery polling cannot hide unresolved jobs or unknown charges. Status timestamps remain unchanged; the command still excludes recent jobs and terminal jobs with known costs.

The final combined fixes passed `pnpm test`: **842 tests in 67 files**, with all disposable local PostgreSQL suites, formatting, lint and TypeScript enabled. The production build passed with 36 static pages, six Workflow steps and one workflow. Provider/storage calls used fixtures; no actual stored objects were deleted and no model requests occurred. Logs: `work/phase2/operations-cleanup-stale-{test,build}.log`. Source `e0182daf81fe22daf11684ecfd9bef1ee802db2e` passed [GitHub CI](https://github.com/transitive-bullshit/share-ai-passage/actions/runs/34955695454) and reached READY as `dpl_7MfcxbXyHJnxQ6VW6jZL6JNqCd9X`, with the stable alias matching. Protection is unchanged; no cleanup Cron is installed, no provider request occurred and production was untouched. Evidence: `work/phase2/hosted-readiness/operations-cleanup-stale-deployment-evidence.json`.

The aggregate operations follow-up adds `pnpm reconcile:operations check` and an explicit optional Resend digest, reusing the existing email transport. It reports stale work, failed/overdue billing reconciliation and cancellation, service-budget admission exhaustion and current-month reservation cost overruns without exposing private fields or repairing data. The final checkout passed formatting, lint, TypeScript, **848 tests in 69 files** with the disposable local PostgreSQL suites, and the production build (36 static pages, six Workflow steps and one workflow). Focused coverage checks actual PostgreSQL aggregation, budget boundaries, notification authorization/configuration, healthy silence, stable day/report/recipient idempotency and sanitized failures; the existing authentication-email tests also pass. Independent source review found no actionable issues. No real email, provider request, scheduler or cloud configuration change ran. Logs: `work/phase2/operations-report-{test,build}.log`. See the [operator command](../GENERATION_RECONCILIATION.md#aggregate-operations-check-and-optional-digest).

The owner subsequently explicitly approved recurring private cleanup and the public Preview exception for development/testing. `GET /api/cron/cleanup-assets` now requires the exact nonblank server-only `CRON_SECRET` before importing cleanup, runs one bounded 100-asset batch, and returns safe counts with no-store/noindex headers. Unauthorized requests return 401; partial or thrown failures return 503. `vercel.json` declares daily 04:00 UTC execution, which Vercel activates only on production deployments. The same new random secret is saved privately in the main `.env.local` and ordinary Vercel Development/Preview settings; production configuration and deployment remain untouched. No per-branch environment override was introduced. See [cleanup operation](../GENERATION_RECONCILIATION.md#daily-private-asset-cleanup) and `work/phase2/hosted-readiness/cleanup-secret-evidence.json`. The separate Preview backup rehearsal remains waived. The combined cleanup changes passed formatting, lint, TypeScript, **858 tests in 70 files** with the disposable local PostgreSQL suites, and the production build (37 generated pages, six Workflow steps and one workflow). Independent review found no actionable issues. New route tests verify authorization before cleanup, bounded counts, partial-failure status and sanitized errors; inherited-secret isolation is covered by the existing production-helper suite. Logs: `work/phase2/cleanup-cron-{test,build}.log`.

## Hosted Preview

**September 16 Preview testing policy:** the owner authorized real API calls with a $5/month image ceiling, image concurrency 1, and a $1/month shared summary ceiling that includes paid sandbox accounts. Vercel authentication is restored on the stable Preview hostname; the September 15 public exception is revoked. The existing Stripe sandbox webhook uses a private automation bypass while keeping signature verification. Production rollout and live checkout remain gated. The optional summary ceiling uses the existing ledger without a migration; with its environment variable unset, prior production behavior is preserved. All 881 tests passed, including concurrent paid/Free admission at the final shared budget slot and unchanged original-period cost settlement. See [current setup](../PRODUCTION.md#accounts-release-readiness). The following September 15 access observations predate this policy.

Commit `01bd7e84dc2150d1edf8818890a6ca8670a473a5` passed CI with **806 tests in 66 files and the production build**. Initial READY deployment `dpl_yGQHNP51AfjzKx4GRDxMEhoQnHUk` uses fresh `passage_accounts_preview` on the no-expiry Neon `preview` branch, reusing `neondb_owner` and ordinary shared Preview variables. All 13 migrations were verified before deployment. Existing development OAuth/Resend, sandbox Stripe and R2 settings are reused; no extra account credential framework was added.

Both hosted queue consumers passed. The exact cancelled-operation workflow reached the Preview database, returned cancelled, retained zero cost/dispatch and unchanged credits, and had all synthetic rows independently verified absent after cleanup. This proves hosted queue → step → Preview DB, not long-running/restart or provider behavior. Evidence: `work/phase2/hosted-readiness/{workflow-health-evidence,cancelled-workflow-state,cancelled-workflow-final-readback}.json`.

A later controlled recovery check passed on READY `e0182da`: the generation step failed once without retry, and recovery completed on its third attempt after the missing synthetic operation was restored as cancelled. The same hosted run completed with zero cost and unchanged credit counters; fresh independent Preview connections verified all fixture rows removed. This establishes durable catch/retry recovery for temporary operation-record unavailability. It does not establish worker termination, a lost paid response, R2 recovery or long-running execution; those are distinct scopes, not evidence supplied by this probe. The first attempt was cancelled after a harness pagination error and is not counted as recovery evidence. See `work/phase2/hosted-readiness/recovery-retry-smoke/{report,attempt-2-state}.json`.

The sandbox webhook has all 20 required events and its endpoint-specific `STRIPE_WEBHOOK_SECRET` saved in shared Preview. Second deployment `dpl_Cquj3ZZEoWpaGtkf7QoLvK4MDeZE` reached READY at 08:38 UTC with the same commit and secret. At that checkpoint, anonymous GET302/POST401 responses from Vercel Standard Protection blocked account entry and signed delivery. The owner subsequently gave explicit approval, and the exact stable-hostname exception was applied and read back on September 15. Project-wide protection remains unchanged; no automation bypass credential was created. Anonymous pricing now returns 200 with Passage plans, the account preferences API rejects missing sessions with 401, and unsigned webhook POST returns 400. The account page itself correctly serves a client-rendered shell with 200. Evidence: `work/phase2/hosted-readiness/{public-preview-exception-evidence,public-preview-http-evidence}.json`. GitHub’s exact stable callback is saved. Google setup and the authenticated hosted flow were subsequently completed as recorded below. Evidence: `work/phase2/hosted-readiness/{deployment-evidence,stripe-deployment-evidence,stripe-webhook-evidence}.json` and the [production setup](../PRODUCTION.md#accounts-release-readiness).

After the approved public exception, one existing `refund.updated` sandbox event (`evt_3UFqX4FCzsOIkvWC05Of4CHk`) was resent to the exact hosted endpoint. Stripe Workbench shows **200 OK at September 15, 10:44:51 UTC** for the stable Preview webhook, and Vercel records the matching POST200 at 10:44:50.983 on `f8bc9a0` / `dpl_DwsW8DNVk1hu2p5Dos5LhqrA66r5`. An independent unsigned POST returned 400. Pre/post checks of the isolated Preview database found zero matching auth users, billing accounts or event rows, as expected for the already deleted synthetic customer; no payment or new fixture was created. This proves hosted signature acceptance and safe unmapped-customer handling, not hosted entitlement changes or renewal. Evidence: `work/phase2/hosted-readiness/{stripe-delivery-report,stripe-runtime-log-evidence}.json`.

The pricing page was visually checked through the owner’s Vercel session. R2 CORS now also includes the exact Preview hostname, preserving the three existing origins.

The owner completed Google Cloud authentication, and the existing development OAuth client now includes the stable Preview origin and exact callback while retaining local HTTPS entries. On READY revision `ca96849beeb170fda90cbda82a93c5c0d04e9af4` / `dpl_Fg6vGyDSeEDZWKRikgaMrM7cTjen`, the browser already had a Preview session with GitHub connected. Google linked to that account; a fresh sign-out/Google sign-in returned successfully, and a full reload preserved both connected methods. Scopes remained email/profile/openid and no credentials changed. The current alias identity was checked via Vercel. This proves authenticated hosted Google behavior through the owner’s Vercel session, not anonymous access or signed Stripe ingress. Email/password had not been configured at that checkpoint. Evidence: `work/phase2/hosted-readiness/google-oauth-evidence.json`. CI also passed for this documentation revision. At source checkpoint `e0182da`, a fresh sign-out/GitHub sign-in returned to the signed-in Preview homepage with My passages and Account navigation. Chrome blocked the subsequent Account check because another extension UI was open; this proves the provider return, not a new post-GitHub account-page reload or password setup. Evidence: `work/phase2/hosted-readiness/github-oauth-evidence.json`.

On September 15, the owner completed hosted password setup and reported successful sign-in with the updated password. A fresh browser reload of Account preserved the signed-in session and showed Email and password, Google and GitHub connected. This verifies the existing-account hosted password flow; fresh signup verification delivery remains untested live. No password or reset token was read or stored in the evidence. Evidence: `work/phase2/hosted-readiness/password-signin-evidence.json`.

The owner waived the separate Preview backup/restore rehearsal. No credentials were read or database connected for that rehearsal, and no dump or scratch database was created. The prepared harness remains ignored and stopped; PostgreSQL 18.6 tools were installed without linking, starting a service or creating a default cluster. This is not a remaining Preview gate.

## Outstanding gates

1. Preview access and the signed webhook smoke are resolved; verify remaining hosted paid entitlement transitions and publication/upload behavior. Existing-account password setup/sign-in now passes; fresh signup verification delivery remains open in the accounts review. Google linking/returning sign-in/reload and the fresh GitHub provider return already pass through the owner’s Vercel session. All recorded sandbox billing lifecycle checks are complete within their stated scope. Production delivery still needs verification before launch; its deferred R2 custom domain does not block Preview checks.
2. Complete saved-HTML review, hosted spending/paid-flow verification and actual hosted billing/operational cost reconciliation, including failure and serving reserves. The live-browser readability and recovery check passes. Reference-subject leakage remains a recorded limitation within the passed sample quality threshold. The specifically approved 25-call batch is complete; no benchmark approval remains pending.
3. Complete provider-spend and hosted operational cost reconciliation, production activation of the declared private-cleanup schedule, operator alert activation, and production backup/restore and migration readiness. Hosted queue/DB identity and controlled recovery retries pass; configured function limits provide headroom for the pinned provider deadline. Deliberately killing workers or running a synthetic 180-second benchmark is not an additional acceptance gate. The aggregate operations check and optional digest are implemented; an operator destination, invocation schedule and actual delivery verification remain setup steps.

Keep `STRIPE_LIVE_CHECKOUT_ENABLED=false` and `IMAGE_GENERATION_ENABLED=0` until the applicable gates pass. Sample image costs are promising but are not a validated worst-case margin or lifetime-serving guarantee. The [measured economics report](../research/PHASE2_MEASURED_ECONOMICS.md), [implementation handoff](../ACCOUNTS_PAID_FEATURES_PLAN.md), [production guide](../PRODUCTION.md#paid-services-and-launch-gate) and [reconciliation guide](../GENERATION_RECONCILIATION.md) contain the supporting contracts and next steps.
