# Paid features review

Updated September 18, 2026. The owner merged PR #4 and deployed accounts, Stripe integration and uploaded-image customization at `fedb92d`. Live Checkout is enabled with explicit owner authorization and a production redeploy. [Historical verification](archive/PAID_REVIEW_2026_09_17.md) preserves detailed earlier evidence; AI image generation, style references and image packs have been removed from the current scope.

## Production review — September 18

Production signup, actual verification-email delivery and verification, password sign-in, guest passage import, durable preparation, one regeneration and publication passed. Both generations settled at a combined $0.000498; Free usage imported correctly at 2 of 25 with no reserved credits. Valid Free template/upload requests return 403, private APIs remain uncached and unauthorized requests are denied. Production pricing marks the owner's Free plan correctly. Existing public readers and social cards return Vercel cache hits; static pricing/privacy, both authenticated production cron schedules and the migration ledger are present.

Google production branding is verified and published. The owner's production Google connection/callback completed and Account shows Google and GitHub connected. The prepared live Stripe webhook is enabled: a Stripe-originated replay of an existing event with no Passage customer mapping is Delivered with `200 OK`, with Passage billing counts unchanged. Invalid signatures are rejected. After the owner's explicit activation instruction, enabled Checkout and redeployed the same production source. Plus/Pro buttons are enabled; the Plus button opens live Passage Plus Checkout at $10/month. API readback confirmed the exact price and production return URLs; the unpaid session was expired without a payment or subscription. Post-redeploy serving and webhook signature checks passed. Google console reconfirmed External/In production with verified branding, and deployed sign-in initialization uses the production client and callback. Complete hosted paid upload/template/publication verification remains open. Receipt: `work/production-launch/billing-activation-evidence.json`.

One follow-up is needed: direct `/create` creation → publication → reader → browser Back restored a blank creation form on production. The router fix at `4880b87` passes the same hosted loop on protected Preview, cold reload and Create another passage, plus 10 focused regressions, lint and TypeScript. It awaits merge/deployment. These production receipts are under ignored `work/production-review/`; the checks below describe earlier revisions.

## Current implementation

Free, Plus and Pro retain $0/$10/$25 monthly prices, $96/$240 annual prices and 25/100/300 monthly summary allowances. Paid customization uses uploaded backgrounds/logos and saved templates with consistent colors, fonts, position and branding. There are no image model calls, image-generation routes/workflows, image credits or pack purchases. Existing completed artwork becomes an ordinary background selection; published card assets stay fixed.

Creation persists an owned passage ID, then runs source reading and summary preparation in Vercel Workflow. `/create?passage=<id>` follows persisted state and shows the published result after publishing, including reopening from browser history. Stale published edits/regenerations are rejected; Revise creates separate work. My passages shows published work before drafts.

## Verification and remaining gates

The pre-merge revision passed **877 tests in 73 files**, including disposable PostgreSQL suites, formatting, lint and TypeScript. The production build passes with six preparation steps and one workflow. Editor regression checks retain fitted canvases and decoded artwork across title/highlight edits, reject stale font readiness, and refit genuine wrapping changes before publication becomes available. Title/highlight length recommendations never reject editing, autosave or publication. Cards clip titles to two lines and highlights to three lines with ellipses, preserving complete saved/reader text. Native renderer regressions verify clipping and painted ellipses for all five styles; oversized text also saves and publishes through disposable PostgreSQL. Eight saved card exports remain byte-identical; their HTML comparison and eight fresh `gpt-5.4-nano` summary pairs were reviewed in Chrome. The isolated Preview has migration `0016_subscription_emails`; existing billing records and publications remained unchanged.

The September 17 rebase onto `main` at `6eb3ed7` retains readable source-thread image capture, private R2 storage, publication-bound media delivery and the configured production image domain. Conversation image import remains available without paid access; paid background/logo uploads use the same storage configuration. Full disposable-database checks and the production build pass after integration. All eight offline WebP exports and saved HTML previews are byte-identical to the pre-rebase baseline (`r2-main-rebase-20260917`); no model calls were used. Public readers and social cards keep their existing cache rules.

Subscription changes now send branded Resend confirmations through a private transactional outbox. Disposable PostgreSQL checks cover duplicate/stale signed-event reconciliation, unpaid price changes, manual-refresh/webhook races, provider failure with paid access committed, overlapping workers, frozen-payload retries, abandoned delivery leases, provider idempotency expiry, legacy subscription baseline, changed recipients and closure. A direct worker smoke check against the isolated Preview database and real development Resend key accepted one activation message to Resend's official delivered test address. A repeated DB delivery found no pending work; two provider replays returned the same receipt. The synthetic user/outbox were removed, and existing billing records/publications were preserved. Protected Preview delivery also accepted two synthetic activation/cancellation confirmations through the authenticated retry endpoint; replay found no pending work, public access stayed protected, and missing cron authorization returned 401. Exact fixtures were removed and existing billing/publications were unchanged. These checks establish real provider acceptance rather than delivery to the owner's inbox; evidence: `work/subscription-emails/preview-smoke.json` and `work/subscription-emails/hosted-smoke.json`. Immediate Stripe cancellation is distinguished from restoration even while invoice payment coverage remains.

Earlier protected Preview checks passed Google/GitHub sign-in and sessions, existing-account email/password, pricing/session state, private APIs, signed Stripe webhook delivery, Workflow routing and controlled recovery. Subscription lifecycle, refunds/disputes, upload ownership, template/card persistence and caching have automated/local real-service coverage. The earlier image qualification and cost report are historical evidence, not launch gates.

On September 17, the protected Preview showed the persisted published result when reopening `/create?passage=<id>`, navigating to its reader and pressing Back, refreshing, and following a legacy `?draft=` link. The template editor offers only curated/uploaded artwork and labels its controls “Background position,” “Horizontal position” and “Vertical position.” The library regression suite also excludes published identities from drafts.

For the simplified scope, the complete hosted paid upload/template/publication journey remains a separate launch check. The owner still reviews fresh email signup/verification and a card on desktop/phone. The owner explicitly declined a Preview backup/restore exercise.

[Preview](https://share-ai-passage-git-codex-accounts-first-launch-saasify.vercel.app) requires Vercel authentication. Sandbox Stripe and `SUMMARY_AI_MONTHLY_BUDGET_USD=1` keep test exposure tight. Live checkout is disabled. Public reader/image cache rules are retained.

## Production preparation — September 17

The owner authorized preparation before merging. A consistent production logical backup and independent PostgreSQL 18 restore passed; all ten pending migrations were rehearsed and then applied to Neon through `0016`. Existing public data and the legacy publication conflict key were preserved. Production Google/Resend/R2/database/OpenAI settings already existed; GitHub credentials, the ordinary cron secret, a $25/month service-wide summary admission ceiling and disabled live Checkout were added to Vercel Production and the main checkout's private `.env.prod.local`. Production Resend accepted an idempotent synthetic delivery-test message, R2 access/CORS checks passed, and the existing production homepage/reader/card returned 200.

Live Passage Plus/Pro products and their four monthly/annual USD prices are created in the existing Agentic account. Their ordinary price-ID variables are saved to Vercel Production and the main private production file. No subscription/payment was created and other products are preserved.

After the owner's explicit approval and Stripe identity verification, created the standard `Passage Production` live key and verified merchant readiness and exact live price terms through the API. Completed the separate nondefault checked-upgrade Portal: only Passage prices, price-only changes, immediate invoicing, unchanged billing anchor and inactive no-code login. Agentic's default management Portal is unchanged. Prepared live webhook `we_1UGs2sFRpaq9IkEXYCIdyRYF` with the exact 20 native reconciliation events and pinned API version; it was initially disabled and is now enabled after handler deployment. The runtime key, separate signing secret and upgrade Portal ID are saved privately in Vercel Production and the main `.env.prod.local`. Other webhooks and Preview environment settings are preserved; no live payment/subscription was created.

The static `/privacy` page and homepage/footer link are now deployed. Production Google uses the published External audience and identity-only scopes; domain ownership and branding are verified. The deployed Stripe handler and enabled live endpoint pass signed delivery. Live Checkout is now active with explicit owner authorization. Preserve the shared Agentic account's other products and settings. Complete hosted paid upload/template/publication verification remains open. See [production guidance](PRODUCTION.md#live-billing-activation--september-18) for activation evidence and the separate follow-up history fix.
